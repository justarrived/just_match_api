require 'rails_helper'

RSpec.describe Api::BaseController, regressor: true do
  # === Routes (REST) ===

  # === Callbacks (Before) ===

  # === Callbacks (After) ===

  # === Callbacks (Around) ===

end
