#!/usr/bin/env ruby
# frozen_string_literal: true
require 'pathname'

# path to your application root.
APP_ROOT = Pathname.new(File.expand_path('../../', __FILE__))

Dir.chdir(APP_ROOT) { system 'scripts/setup' }
