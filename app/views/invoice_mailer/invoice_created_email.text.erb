<%= I18n.t(
  'mailer.invoice_created.body',
  name: @user_name,
  job_name: @job_name,
  payslip_explain_url: @payslip_explain_url,
  support_email: @support_email
) %>

<%= I18n.t('mailer.signoff') %>
