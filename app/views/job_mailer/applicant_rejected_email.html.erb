
<%= simple_format I18n.t(
  'mailer.applicant_rejected.body',
  user_name: @user_name,
  job_name: @job_name,
  support_email: @support_email,
  faq_link: @faq_link
) %>

<%= render(
  'mailer_partials/action_button',
  url: @jobs_url,
  title: I18n.t('mailer.applicant_rejected.find_jobs_cta'),
  align: I18nMeta.align(I18n.locale)
) %>


<%= I18n.t('mailer.signoff') %>
