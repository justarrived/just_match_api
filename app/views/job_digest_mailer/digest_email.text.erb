<%= I18n.t('mailer.job_digest.header') %>

<%= I18n.t('mailer.job_digest.body') %>

<% @jobs.each do |job| %>
  <%= job.translated_name %>
  <%= job.translated_short_description %>
  <%= I18n.t('mailer.job_digest.search_job') %>
  <%= frontend_mail_url(:job, id: job.id, utm_campaign: 'job_digest') %>

<% end %>

<%= I18n.t('mailer.job_digest.more_jobs') %>
<%= @more_jobs_url %>

<%= I18n.t('mailer.job_digest.unsubscribe') %>
<%= @unsubscribe_url %>

<%= I18n.t('mailer.signoff') %>
