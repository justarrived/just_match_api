<%= form_for(@job_skill) do |f| %>
  <% if @job_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_skill.errors.count, "error") %> prohibited this job_skill from being saved:</h2>

      <ul>
      <% @job_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br>
    <%= f.text_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.text_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
