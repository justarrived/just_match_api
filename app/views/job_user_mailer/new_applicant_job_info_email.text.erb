<%= I18n.t(
  'mailer.new_applicant_job_info.body',
  name: @user_name,
  job_name: @job_name
) %>

<% unless @competence_names.empty? %>
<%= I18n.t('mailer.new_applicant_job_info.body_fillout_notice') %>

  <% @competence_names.each do |name| %>
  <%= name %>
  <% end %>
<% end %>
<% if @missing_cv %>
<%= I18n.t('mailer.new_applicant_job_info.body_cv_fillout_notice') %>

<% end %>
<%= I18n.t(
  'mailer.new_applicant_job_info.body_edit_profile',
  url: @user_edit_url
) %>

<%= I18n.t('mailer.signoff') %>
